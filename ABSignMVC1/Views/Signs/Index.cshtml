@model IEnumerable<ABSignMVC1.Sign>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Image)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FancyText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Image)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FancyText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SignID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SignID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SignID })
        </td>
    </tr>
}

</table>
